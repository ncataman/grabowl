# Node 25 exposes globalThis.localStorage as a getter that throws unless a backing
# file is configured. WXT touches it while setting up the extension environment,
# so every npm script needs this flag. Harmless on older Node versions.
node-options=--localstorage-file=.wxt/localstorage
